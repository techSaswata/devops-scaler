
=== 1. WHY CONFIGMAPS EXIST ===
  Without one, configuration is baked into the image:

      ENV ENVIRONMENT=production        # in the Dockerfile

  That means a separate image per environment, a rebuild to change a log level,
  and no way to see current config without cracking the image open.

  A ConfigMap decouples config from the image: ONE image, many environments.

=== 2. CREATE THE CONFIGMAP  [checklist 1] ===

$ cat /Users/techsaswata/Downloads/devops-scaler/11-k8s-ingress-configmaps-secrets/manifests/01-configmap.yaml | head -14
# Plain-text configuration, decoupled from the container image.
# Nothing sensitive belongs here - see 02-secret.yaml.
apiVersion: v1
kind: ConfigMap
metadata:
  name: yatri-app-config
  namespace: default
  labels: { app: yatri-app }
data:
  ENVIRONMENT: "production"
  LOG_LEVEL: "INFO"
  APP_PORT: "5000"
  DEFAULT_CURRENCY: "INR"
  MAX_BOOKING_DAYS: "30"

$ kubectl apply -f /Users/techsaswata/Downloads/devops-scaler/11-k8s-ingress-configmaps-secrets/manifests/01-configmap.yaml
configmap/yatri-app-config created
configmap/yatri-frontend-html created

$ kubectl get configmap
NAME                  DATA   AGE
kube-root-ca.crt      1      97m
yatri-app-config      5      0s
yatri-frontend-html   1      0s

--- describe shows the VALUES in plain text (they are not secret) ---

$ kubectl describe configmap yatri-app-config
Name:         yatri-app-config
Namespace:    default
Labels:       app=yatri-app
Annotations:  <none>

Data
====
APP_PORT:
----
5000

DEFAULT_CURRENCY:
----
INR

ENVIRONMENT:
----
production

LOG_LEVEL:
----
INFO

MAX_BOOKING_DAYS:
----
30


BinaryData
====

--- read ONE key with jsonpath  [checklist 1] ---

$ kubectl get configmap yatri-app-config -o jsonpath='{.data.DEFAULT_CURRENCY}{"\n"}'
INR

$ kubectl get configmap yatri-app-config -o jsonpath='{.data.MAX_BOOKING_DAYS}{"\n"}'
30

--- the imperative equivalents (useful, but prefer files in git) ---
$ kubectl create configmap demo --from-literal=KEY=value
$ kubectl create configmap demo --from-file=./app.properties
$ kubectl create configmap demo --from-env-file=./.env

=== 3. CREATE THE SECRET  [checklist 2] ===

$ cat /Users/techsaswata/Downloads/devops-scaler/11-k8s-ingress-configmaps-secrets/manifests/02-secret.yaml | tail -10
  namespace: default
  labels: { app: yatri-app }
type: Opaque
data:
  # echo -n "yatri_admin"         | base64
  POSTGRES_USER: eWF0cmlfYWRtaW4=
  # echo -n "secretpassword"      | base64
  POSTGRES_PASSWORD: c2VjcmV0cGFzc3dvcmQ=
  # echo -n "yatri_production_db" | base64
  POSTGRES_DB: eWF0cmlfcHJvZHVjdGlvbl9kYg==

$ kubectl apply -f /Users/techsaswata/Downloads/devops-scaler/11-k8s-ingress-configmaps-secrets/manifests/02-secret.yaml
secret/yatri-db-secret created

$ kubectl get secret yatri-db-secret
NAME              TYPE     DATA   AGE
yatri-db-secret   Opaque   3      0s
>> TYPE=Opaque means arbitrary user-defined key/value data.

--- describe HIDES the values ---

$ kubectl describe secret yatri-db-secret
Name:         yatri-db-secret
Namespace:    default
Labels:       app=yatri-app
Annotations:  <none>

Type:  Opaque

Data
====
POSTGRES_DB:        19 bytes
POSTGRES_PASSWORD:  14 bytes
POSTGRES_USER:      11 bytes
>> Only byte counts. This is why people assume Secrets are encrypted.

=== 4. BASE64 IS NOT ENCRYPTION  [checklist 2] ===
--- the stored value ---

$ kubectl get secret yatri-db-secret -o jsonpath='{.data.POSTGRES_PASSWORD}{"\n"}'
c2VjcmV0cGFzc3dvcmQ=

--- decode it. Anyone with read access can do this. ---
$ kubectl get secret yatri-db-secret -o jsonpath='{.data.POSTGRES_PASSWORD}' | base64 --decode
secretpassword


$ kubectl get secret yatri-db-secret -o jsonpath='{.data.POSTGRES_USER}' | base64 --decode
yatri_admin

  >> Base64 is an ENCODING, not encryption. It exists so binary values
  >> (certificates, keys) can live inside YAML - not to hide anything.

  WHAT ACTUALLY PROTECTS A SECRET:
    - RBAC: control who can `get secrets` at all
    - encryption at rest: EncryptionConfiguration on the API server, so etcd
      does not hold plaintext
    - an external store: Vault, AWS/GCP Secrets Manager, Sealed Secrets
    - NEVER commit a real Secret manifest to git - the base64 is not a defence


=== 5. THE echo vs echo -n NEWLINE BUG  [checklist 9] ===
--- WRONG: plain echo appends a trailing newline ---
$ echo 'secretpassword' | base64
c2VjcmV0cGFzc3dvcmQK

--- RIGHT: -n suppresses it ---
$ echo -n 'secretpassword' | base64
c2VjcmV0cGFzc3dvcmQ=

--- the two encodings differ ---
  with newline : c2VjcmV0cGFzc3dvcmQK
  without      : c2VjcmV0cGFzc3dvcmQ=

--- decode both and count the bytes ---
$ echo 'c2VjcmV0cGFzc3dvcmQK' | base64 --decode | wc -c
      15
$ echo 'c2VjcmV0cGFzc3dvcmQ=' | base64 --decode | wc -c
      14

--- show the invisible character ---
$ echo 'c2VjcmV0cGFzc3dvcmQK' | base64 --decode | od -c | head -2
0000000    s   e   c   r   e   t   p   a   s   s   w   o   r   d  \n    
0000017

  >> 15 bytes vs 14. The extra \n is INSIDE the password.
  >> The app sends "secretpassword\n" to PostgreSQL, authentication fails, and
  >> the error says only "password authentication failed for user" - it never
  >> mentions a newline. People lose hours to this.
  >>
  >> ALWAYS use `echo -n`, or avoid the problem entirely:
  >>    kubectl create secret generic db --from-literal=PASSWORD='secretpassword'
  >> (--from-literal does the encoding for you, with no newline)

=== 6. INJECT INTO THE BACKEND  [checklist 3] ===

$ grep -A14 'envFrom:' /Users/techsaswata/Downloads/devops-scaler/11-k8s-ingress-configmaps-secrets/manifests/03-backend.yaml | head -18
          # envFrom: import EVERY key in the ConfigMap as an env var
          envFrom:
            - configMapRef:
                name: yatri-app-config
          # env + secretKeyRef: import SPECIFIC keys from the Secret
          env:
            - name: POSTGRES_USER
              valueFrom: { secretKeyRef: { name: yatri-db-secret, key: POSTGRES_USER } }
            - name: POSTGRES_PASSWORD
              valueFrom: { secretKeyRef: { name: yatri-db-secret, key: POSTGRES_PASSWORD } }
            - name: POSTGRES_DB
              valueFrom: { secretKeyRef: { name: yatri-db-secret, key: POSTGRES_DB } }
          resources:
            requests: { cpu: "50m", memory: "64Mi" }
            limits:   { cpu: "200m", memory: "128Mi" }
---

  TWO INJECTION STYLES ON ONE POD:

    envFrom: configMapRef   imports EVERY key in the ConfigMap as an env var.
                            Convenient, but you cannot see the variable names
                            by reading the Deployment.

    env: + secretKeyRef     imports ONE specific key, and lets you rename it.
                            Explicit, and the usual choice for secrets.

$ kubectl apply -f /Users/techsaswata/Downloads/devops-scaler/11-k8s-ingress-configmaps-secrets/manifests/03-backend.yaml
deployment.apps/yatri-backend created
service/yatri-backend-service created
Waiting for deployment "yatri-backend" rollout to finish: 0 of 2 updated replicas are available...
Waiting for deployment "yatri-backend" rollout to finish: 1 of 2 updated replicas are available...
deployment "yatri-backend" successfully rolled out

$ kubectl get pods -l app=yatri-backend
NAME                             READY   STATUS    RESTARTS   AGE
yatri-backend-56bcc95f79-2rsvz   1/1     Running   0          12s
yatri-backend-56bcc95f79-cbdpr   1/1     Running   0          12s

--- verify the env vars INSIDE the pod  [checklist 3] ---

$ kubectl exec yatri-backend-56bcc95f79-2rsvz -- env | grep -E 'ENVIRONMENT|LOG_LEVEL|DEFAULT_CURRENCY|MAX_BOOKING_DAYS' | sort
DEFAULT_CURRENCY=INR
ENVIRONMENT=production
LOG_LEVEL=INFO
MAX_BOOKING_DAYS=30
>> ...from the ConfigMap via envFrom.


$ kubectl exec yatri-backend-56bcc95f79-2rsvz -- env | grep -E 'POSTGRES_' | sort
POSTGRES_DB=yatri_production_db
POSTGRES_PASSWORD=secretpassword
POSTGRES_USER=yatri_admin
>> ...from the Secret via secretKeyRef - DECODED automatically.
>> Note the pod sees the PLAINTEXT. Base64 is only the storage format.

--- and the password is exactly 14 bytes, so no newline bug here ---

$ kubectl exec yatri-backend-56bcc95f79-2rsvz -- sh -c 'printf "%s" "$POSTGRES_PASSWORD" | wc -c'
14

=== 7. DEPLOY THE FRONTEND  [checklist 4] ===

$ kubectl apply -f /Users/techsaswata/Downloads/devops-scaler/11-k8s-ingress-configmaps-secrets/manifests/04-frontend.yaml
deployment.apps/yatri-frontend created
service/yatri-frontend-service created
Waiting for deployment "yatri-frontend" rollout to finish: 0 of 2 updated replicas are available...
Waiting for deployment "yatri-frontend" rollout to finish: 1 of 2 updated replicas are available...
deployment "yatri-frontend" successfully rolled out

$ kubectl get pods -l app=yatri-frontend
NAME                              READY   STATUS    RESTARTS   AGE
yatri-frontend-77b487969f-n48mq   1/1     Running   0          1s
yatri-frontend-77b487969f-zz6h4   1/1     Running   0          1s

--- the ConfigMap mounted as a VOLUME becomes a FILE ---

$ kubectl exec yatri-frontend-77b487969f-n48mq -- ls -l /usr/share/nginx/html/
total 0
lrwxrwxrwx    1 root     root            17 Sep 17 19:28 index.html -> ..data/index.html

$ kubectl exec yatri-frontend-77b487969f-n48mq -- head -4 /usr/share/nginx/html/index.html
<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8">
>> Each KEY in the ConfigMap became a FILE named after that key.

--- both services are ClusterIP  [checklist 4] ---

$ kubectl get svc yatri-backend-service yatri-frontend-service
NAME                     TYPE        CLUSTER-IP      EXTERNAL-IP   PORT(S)   AGE
yatri-backend-service    ClusterIP   10.96.181.173   <none>        80/TCP    14s
yatri-frontend-service   ClusterIP   10.96.106.121   <none>        80/TCP    2s
>> ClusterIP: neither is reachable from outside. The Ingress will be the
>> single entry point - that is the whole point of part 2.
